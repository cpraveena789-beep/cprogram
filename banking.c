#include <stdio.h>

int main() {
    int a=1000;
    if(a<1000)
    {
        printf("Cash withdraw successfully"); 
        if(a=1000)
        {
            printf(" Cash withdraw with fine");
        }
    }
    else{
        printf("Cash withdraw failed");
    }
    
    return 0;
}